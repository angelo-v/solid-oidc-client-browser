/**
 * Subresource Integrity hash of the built RefreshWorker bundle
 * (dist/esm/web/RefreshWorker.js), consumed by SecureSharedWorker.
 *
 * This is a placeholder: rollup replaces the token with the real hash
 * when emitting the web bundle (see rollup.config.js). The real value
 * only exists in the build output.
 */
export const REFRESH_WORKER_INTEGRITY = 'sha384-__REFRESH_WORKER_INTEGRITY__';
